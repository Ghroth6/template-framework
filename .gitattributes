# ============================================================================
# TFW项目文件编码和行尾符配置
# 基于实际文件检测结果生成
# ============================================================================

# 全局设置：默认使用UTF-8编码，行尾符为LF
* text=auto eol=lf

# ============================================================================
# 源代码文件 - 检测结果显示大部分为UTF-8 with BOM
# ============================================================================

# C/C++头文件 - 检测为UTF-8 with BOM
*.h text eol=lf working-tree-encoding=UTF-8
*.hpp text eol=lf working-tree-encoding=UTF-8

# C/C++源文件 - 检测为UTF-8 with BOM (除了core/TFW_core.cpp)
*.c text eol=lf working-tree-encoding=UTF-8
*.cpp text eol=lf working-tree-encoding=UTF-8

# CMake配置文件 - 检测为UTF-8 with BOM
*.cmake text eol=lf working-tree-encoding=UTF-8
CMakeLists.txt text eol=lf working-tree-encoding=UTF-8

# ============================================================================
# 脚本文件 - 检测为UTF-8 with BOM
# ============================================================================

# Shell脚本 - 检测为UTF-8 with BOM
*.sh text eol=lf working-tree-encoding=UTF-8

# Windows批处理文件 - 检测为UTF-8 with BOM
*.bat text eol=lf working-tree-encoding=UTF-8
*.cmd text eol=lf working-tree-encoding=UTF-8
*.ps1 text eol=lf working-tree-encoding=UTF-8

# ============================================================================
# 文档文件 - 检测为UTF-8 with BOM
# ============================================================================

# Markdown文档 - 检测为UTF-8 with BOM
*.md text eol=lf working-tree-encoding=UTF-8

# 文本文件 - 检测为UTF-8 with BOM
*.txt text eol=lf working-tree-encoding=UTF-8

# ============================================================================
# 二进制文件 - 不进行文本转换
# ============================================================================

# 可执行文件和库文件
*.exe binary
*.dll binary
*.so binary
*.dylib binary
*.a binary
*.lib binary

# 编译产物
*.o binary
*.obj binary

# ============================================================================
# 特殊文件类型
# ============================================================================

# 日志文件
*.log text eol=lf working-tree-encoding=UTF-8

# 配置文件
*.ini text eol=lf working-tree-encoding=UTF-8
*.cfg text eol=lf working-tree-encoding=UTF-8
*.conf text eol=lf working-tree-encoding=UTF-8

# ============================================================================
# 编码说明
# ============================================================================
# 项目中的大部分文件检测为UTF-8 with BOM编码
# 这确保了在Windows系统上的最佳兼容性
# 同时保持了跨平台的一致性
# ============================================================================
